// ap.js

// Initialize Firebase (uses firebaseConfig from config.js)
const app = firebase.initializeApp(firebaseConfig);
const auth = firebase.ath();
const db = firebase.firestore();

console.log("Firebase initialized!");
